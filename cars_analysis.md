# 车辆数据可视化分析系统  
## 项目文档

### 1. 项目概述

#### 1.1 项目目标
- 实现前后端分离的轻量级Web数据可视化系统
- 支持年份趋势、区域分布、品牌/车型/性别等多维度统计图表展示
- 引入Apriori算法进行关联规则挖掘，发现隐藏的消费者行为模式
- 系统界面美观、响应迅速、易于扩展

#### 1.2 项目意义
- 将数据可视化与数据挖掘相结合，提升分析深度
- 采用前后端分离架构，便于后期维护与功能扩展
- 为汽车行业提供一个可参考的数据分析模板

### 2. 系统功能介绍

系统共包含6个主要可视化模块：

1. **年份趋势分析**：折线图展示车辆数量随年份的变化趋势，帮助识别市场增长阶段。
2. **城市分布分析**：柱状图展示Top 20城市的车辆数量，帮助识别热点区域。
3. **品牌占比分析**：饼图展示前10大品牌占比，并将剩余品牌归类为“其他”，避免图表碎片化。
4. **车型分布分析**：饼图展示不同车型（mold）的分布情况。
5. **性别分布分析**：饼图展示车主性别比例。
6. **关联规则挖掘**：通过Apriori算法挖掘province、city、brand、mold、sex五个维度之间的强关联规则，并以表格形式展示Top 15条最强的规则（包含支持度、置信度、提升度）。

### 3. 系统架构设计

#### 3.1 整体架构
系统采用经典的**前后端分离**架构：

```
浏览器（前端）
    ├── HTML + CSS + JavaScript
    ├── ECharts 图表库
    └── Fetch API 异步请求数据
         ↓
后端（Flask）
    ├── 提供 RESTful API 接口（/api/*）
    ├── pymysql 操作 MySQL 数据库
    └── pandas + mlxtend 进行关联规则挖掘
         ↓
数据库（MySQL）
    └── car_raw 表（包含 year, province, city, brand, mold, sex 等字段）
```

#### 3.2 技术栈
| 层级   | 技术/框架/库                          | 用途                              |
|--------|---------------------------------------|-----------------------------------|
| 后端   | Python Flask                          | Web框架，提供API接口              |
|        | pymysql                               | MySQL数据库连接                   |
|        | pandas                                | 数据处理与One-Hot编码             |
|        | mlxtend（apriori & association_rules）| 关联规则挖掘                      |
| 前端   | HTML5 + CSS3 + 原生JavaScript         | 页面结构与交互                    |
|        | ECharts 5.x                           | 图表渲染                          |
| 数据库 | MySQL                                 | 数据存储                          |

优势：架构清晰、轻量级、易部署、扩展性强。

### 4. 数据库设计

#### 4.1 数据表结构（car_raw）
以下是这份数据的**完整表结构**，包含字段名、推测的数据类型（基于样本数据特征）、字段含义说明，覆盖所有列：

| 字段名   | 数据类型 | 字段含义说明                                                                 |
|----------|--------------|------------------------------------------------------------------------------|
| province | 字符串（文本） | 车辆所属省份（如：山西省）                                                   |
| month    | 整数         | 月份（如：3、12）                                                           |
| city     | 字符串（文本） | 车辆所属城市（如：朔州市、晋城市、长治市）                                   |
| county   | 字符串（文本） | 车辆所属区县（如：朔城区、城区、长治城区）                                   |
| year     | 整数         | 年份（如：2013）                                                             |
| cartype  | 字符串（文本） | 车辆公告型号（如：LZW6450PF、EQ6450PF1）                                     |
| productor| 字符串（文本） | 车辆生产厂家（如：上汽通用五菱汽车股份有限公司、东风小康汽车有限公司）       |
| brand    | 字符串（文本） | 汽车品牌（如：五菱、东风、北京、航天）                                       |
| mold     | 字符串（文本） | 车辆类型（如：小型普通客车、中型普通客车、大型普通客车）                     |
| owner    | 字符串（文本） | 车主类型（个人/单位）                                                       |
| nature   | 字符串（文本） | 车辆营运性质（非营运/公路客运等）                                           |
| number   | 整数         | 数量                                                                        |
| ftype    | 字符串（文本） | 发动机型号（如：L3C、DK13-06、BJ415A）                                       |
| outv     | 整数/空值    | 发动机排量（单位：cc）                                                        |
| power    | 浮点数       | 发动机功率（单位：kW）                                                       |
| fuel     | 字符串（文本） | 燃料类型（汽油/柴油/天然气，部分记录为空）                                   |
| length   | 浮点数/整数/空值 | 车辆长度（单位：mm）                                                      |
| width    | 浮点数/整数/空值 | 车辆宽度（单位：mm）                                                     |
| height   | 浮点数/整数/空值 | 车辆高度（单位：mm）                                                      |
| xlength  | 字符串/空值   | 未知维度长度（样本中几乎为空）                                               |
| xwidth   | 字符串/空值   | 未知维度宽度（样本中几乎为空）                                               |
| xheight  | 字符串/空值   | 未知维度高度（样本中几乎为空）                                               |
| count    | 整数/空值    | 未知计数字段（样本中多为2，部分记录为空）                                   |
| base     | 整数/空值    | 车辆轴距（单位：mm，如：3050、2725，部分记录为空）                           |
| front    | 整数/空值    | 车辆前轮距（单位：mm，如：1386、1435，部分记录为空）                         |
| norm     | 字符串（文本） | 轮胎规格（如：175/70R14LT、205/75R16C，部分记录为空）                       |
| tnumber  | 整数/空值    | 轮胎数量（如：4、6，部分记录为空）                                           |
| total    | 整数/空值    | 车辆总质量（单位：kg，如：2110、3980，部分记录为空）                         |
| curb     | 整数/空值    | 车辆整备质量（单位：kg，如：1275、2550，部分记录为空）                       |
| hcurb    | 整数/空值    | 核定载质量/相关质量字段（样本中多为空）                                     |
| passenger| 字符串（文本） | 核定载客人数）                                                          |
| zhcurb   | 整数/空值    | 未知质量相关字段（样本中多为0或空）                                         |
| business | 字符串/空值   | 业务类型/备注字段（样本中多为空）                                           |
| dtype    | 字符串（文本） | 车辆类型细分                                                             |
| fmold    | 字符串（文本） | 发动机相关型号                                                           |
| fbusiness| 字符串（文本） | 发动机生产厂家（如：重庆渝安淮海动力有限公司、柳州五菱柳机动力有限公司）     |
| name     | 字符串/空值   | 车辆名称                                                                  |
| age      | 整数/空值    | 车主年龄                                                                  |
| sex      | 字符串（文本） | 车主性别（男性/女性/空）                                                  |


### 5. 后端实现详解（app.py）

#### 5.1 数据库连接封装
使用`query_db()`函数统一处理数据库查询，包含异常捕获与连接关闭，确保资源安全。

#### 5.2 API 接口列表
| 接口路径                | 方法 | 功能描述                        | 返回数据格式 |
|-------------------------|------|---------------------------------|--------------|
| /                       | GET  | 渲染主页面（dashboard.html）    | HTML         |
| /api/year               | GET  | 按年份统计车辆数量              | JSON数组     |
| /api/province           | GET  | 按城市统计（Top 20）            | JSON数组     |
| /api/brand              | GET  | 按品牌统计（Top 10 + 其他）     | JSON数组     |
| /api/mold               | GET  | 按车型统计                      | JSON数组     |
| /api/sex                | GET  | 按性别统计                      | JSON数组     |
| /api/association_rules  | GET  | 关联规则挖掘（Top 15强规则）    | JSON数组     |

#### 5.3 关联规则挖掘核心流程
1. 从数据库查询最多80,000条有效记录（过滤空值）。
2. 使用pandas将类别变量进行One-Hot编码。
3. **关键优化**：重命名列，去掉“province_”“city_”等前缀，使项集名称更直观（如直接显示“北京市”而非“city_北京市”）。
4. 使用mlxtend的apriori算法挖掘频繁项集（min_support=0.008, low_memory=True）。
5. 生成关联规则（min_confidence=0.5）。
6. 过滤提升度>1.5的强规则。
7. **去重处理**：A→B 与 B→A 只保留置信度更高的一条（通过frozenset实现）。
8. 按提升度、置信度降序排序，取Top 15条。
9. 返回前端友好的JSON结构。

### 6. 前端实现详解（dashboard.html）

#### 6.1 页面布局
- 使用Flex布局实现响应式网格（大屏2列，小屏1列）。
- 每个图表放在独立的`.chart-box`中，带阴影与圆角，美观大方。

#### 6.2 图表渲染
- 所有图表均使用`fetch()`异步获取JSON数据后调用ECharts渲染。
- 图表类型选择合理：
  - 趋势 → 折线图
  - 排名 → 柱状图
  - 占比 → 饼图（环形）
- 支持窗口resize自适应。

#### 6.3 关联规则展示
- 不使用图表，而是动态生成HTML表格，更适合展示文本规则。
- 前件（antecedents）用蓝色徽章，后件（consequents）用红色徽章区分。
- 提升度根据数值大小使用不同颜色（>10深红、>5橙色、>2绿色）。
- 无强规则时显示友好提示。

### 7. 系统运行效果

启动方式：
```bash
python app.py
```
访问地址：http://127.0.0.1:5000

访问地址：http://192.168.254.129:5000

页面效果：
- 顶部标题清晰
- 6个模块整齐排列
- 图表加载迅速、交互流畅
- 关联规则表格美观易读

### 8. 系统特点与创新点

---

#### 为什么选Flask而非Django？（贴合你的车辆数据项目）
你的核心需求是「轻量数据分析API + 可视化仪表盘」，而非复杂后台系统，Flask的优势完全适配这个场景，具体对比如下：

| 维度                | Flask| Django|
|---------------------|---------------------------------------------|---------------------------------------------|
| **场景适配**        | 仅需6个API + 1个首页，无用户认证/权限等复杂功能，极简结构（单文件可跑），开发/调试效率高 | 内置admin/form/auth等冗余模块，需按MTV架构分层，增加无意义的代码量 |
| **定制化灵活度**    | 可直接在路由中写定制化逻辑（如关联规则的One-hot编码、规则去重），无需适配框架约束 | 强制Model-View-Template分层，你的原生SQL/pandas分析逻辑需额外适配ORM，限制灵活性 |
| **资源消耗**        | 启动仅占几十MB内存，适配8万条车辆数据的轻量分析场景 | 启动加载大量内置模块，内存占用是Flask的3-5倍，挤占数据分析的计算资源 |
| **扩展轻量化**      | 直接封装`query_db`函数执行原生SQL，适配复杂的GROUP BY/LIMIT查询 | Django ORM虽强大，但你的代码中用原生SQL更灵活，ORM反而增加学习/适配成本 |



1. **前后端分离**：接口与展示完全解耦，便于后续开发移动端或替换前端框架。
2. **智能“其他”类别处理**：品牌饼图避免长尾碎片化。
3. **关联规则深度优化**：
   - 列名美化
   - 双向规则去重
   - 多维度阈值过滤
   - 视觉化颜色提示
4. **用户体验友好**：空数据处理、加载提示、响应式布局。

### 9. 系统不足与改进方向

#### 9.1 现有不足
- 数据量较大时关联规则计算较慢（Apriori算法本身复杂度较高）。
- 未实现分页或实时刷新。
- 未加入用户认证与权限管理。
- 未使用地图展示地域分布。

#### 9.2 未来改进方向
1. 算法优化：改用FP-Growth算法提升挖掘效率。
2. 加入ECharts地图组件，实现省份/城市热力图。
3. 支持动态筛选（如选择年份范围后刷新图表）。
4. 部署到云服务器（如阿里云、Vercel+Flask），提供在线访问。
5. 增加更多维度（如价格区间、颜色、使用性质等）的挖掘。
6. 前端框架升级（如使用Vue/React提升交互性）。


