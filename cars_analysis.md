# 车辆数据可视化分析系统  
## 项目文档

### 1. 项目概述

#### 1.1 项目背景
随着汽车市场的快速发展，车辆相关数据（如登记、上牌、销售数据）呈现爆炸式增长。传统的数据查看方式（如Excel表格、简单报表）已无法满足快速、直观地发现数据规律的需求。通过数据可视化和数据挖掘技术，可以帮助汽车经销商、制造商、政府监管部门等快速了解市场分布、消费者偏好以及潜在的关联模式，从而支持精准营销、库存管理与政策制定。

本项目基于真实的车辆基础数据表（car_raw），实现了多维度统计可视化与关联规则挖掘，帮助用户从“宏观趋势”和“微观关联”两个层面深入理解数据。

#### 1.2 项目目标
- 实现前后端分离的轻量级Web数据可视化系统
- 支持年份趋势、区域分布、品牌/车型/性别等多维度统计图表展示
- 引入Apriori算法进行关联规则挖掘，发现隐藏的消费者行为模式
- 系统界面美观、响应迅速、易于扩展

#### 1.3 项目意义
- 将数据可视化与数据挖掘相结合，提升分析深度
- 采用前后端分离架构，便于后期维护与功能扩展
- 为汽车行业提供一个可参考的数据分析模板

### 2. 系统功能介绍

系统共包含6个主要可视化模块：

1. **年份趋势分析**：折线图展示车辆数量随年份的变化趋势，帮助识别市场增长阶段。
2. **城市分布分析**：柱状图展示Top 20城市的车辆数量，帮助识别热点区域。
3. **品牌占比分析**：饼图展示前10大品牌占比，并将剩余品牌归类为“其他”，避免图表碎片化。
4. **车型分布分析**：饼图展示不同车型（mold）的分布情况。
5. **性别分布分析**：饼图展示车主性别比例。
6. **关联规则挖掘**：通过Apriori算法挖掘province、city、brand、mold、sex五个维度之间的强关联规则，并以表格形式展示Top 15条最强的规则（包含支持度、置信度、提升度）。

### 3. 系统架构设计

#### 3.1 整体架构
系统采用经典的**前后端分离**架构：

```
浏览器（前端）
    ├── HTML + CSS + JavaScript
    ├── ECharts 图表库
    └── Fetch API 异步请求数据
         ↓
后端（Flask）
    ├── 提供 RESTful API 接口（/api/*）
    ├── pymysql 操作 MySQL 数据库
    └── pandas + mlxtend 进行关联规则挖掘
         ↓
数据库（MySQL）
    └── car_raw 表（包含 year, province, city, brand, mold, sex 等字段）
```

#### 3.2 技术栈
| 层级   | 技术/框架/库                          | 用途                              |
|--------|---------------------------------------|-----------------------------------|
| 后端   | Python Flask                          | Web框架，提供API接口              |
|        | pymysql                               | MySQL数据库连接                   |
|        | pandas                                | 数据处理与One-Hot编码             |
|        | mlxtend（apriori & association_rules）| 关联规则挖掘                      |
| 前端   | HTML5 + CSS3 + 原生JavaScript         | 页面结构与交互                    |
|        | ECharts 5.x                           | 图表渲染                          |
| 数据库 | MySQL                                 | 数据存储                          |

优势：架构清晰、轻量级、易部署、扩展性强。

### 4. 数据库设计

#### 4.1 数据表结构（car_raw）
| 字段名     | 类型         | 说明                 |
|------------|--------------|----------------------|
| id         | INT          | 主键，自增           |
| year       | INT          | 上牌/登记年份        |
| province   | VARCHAR(50)  | 省份                 |
| city       | VARCHAR(50)  | 地级市               |
| brand      | VARCHAR(50)  | 车辆品牌             |
| mold       | VARCHAR(100) | 车型（如SUV、轿车等）|
| sex        | VARCHAR(10)  | 车主性别（男/女）    |
| ...        | ...          | 其他字段（本项目未使用）|

数据量：数万至数十万条（项目中限制挖掘时最多使用80,000条以保证性能）。

### 5. 后端实现详解（app.py）

#### 5.1 数据库连接封装
使用`query_db()`函数统一处理数据库查询，包含异常捕获与连接关闭，确保资源安全。

#### 5.2 API 接口列表
| 接口路径                | 方法 | 功能描述                        | 返回数据格式 |
|-------------------------|------|---------------------------------|--------------|
| /                       | GET  | 渲染主页面（dashboard.html）    | HTML         |
| /api/year               | GET  | 按年份统计车辆数量              | JSON数组     |
| /api/province           | GET  | 按城市统计（Top 20）            | JSON数组     |
| /api/brand              | GET  | 按品牌统计（Top 10 + 其他）     | JSON数组     |
| /api/mold               | GET  | 按车型统计                      | JSON数组     |
| /api/sex                | GET  | 按性别统计                      | JSON数组     |
| /api/association_rules  | GET  | 关联规则挖掘（Top 15强规则）    | JSON数组     |

#### 5.3 关联规则挖掘核心流程（亮点）
1. 从数据库查询最多80,000条有效记录（过滤空值）。
2. 使用pandas将类别变量进行One-Hot编码。
3. **关键优化**：重命名列，去掉“province_”“city_”等前缀，使项集名称更直观（如直接显示“北京市”而非“city_北京市”）。
4. 使用mlxtend的apriori算法挖掘频繁项集（min_support=0.008, low_memory=True）。
5. 生成关联规则（min_confidence=0.5）。
6. 过滤提升度>1.5的强规则。
7. **去重处理**：A→B 与 B→A 只保留置信度更高的一条（通过frozenset实现）。
8. 按提升度、置信度降序排序，取Top 15条。
9. 返回前端友好的JSON结构。

### 6. 前端实现详解（dashboard.html）

#### 6.1 页面布局
- 使用Flex布局实现响应式网格（大屏2列，小屏1列）。
- 每个图表放在独立的`.chart-box`中，带阴影与圆角，美观大方。

#### 6.2 图表渲染
- 所有图表均使用`fetch()`异步获取JSON数据后调用ECharts渲染。
- 图表类型选择合理：
  - 趋势 → 折线图
  - 排名 → 柱状图
  - 占比 → 饼图（环形）
- 支持窗口resize自适应。

#### 6.3 关联规则展示
- 不使用图表，而是动态生成HTML表格，更适合展示文本规则。
- 前件（antecedents）用蓝色徽章，后件（consequents）用红色徽章区分。
- 提升度根据数值大小使用不同颜色（>10深红、>5橙色、>2绿色）。
- 无强规则时显示友好提示。

### 7. 系统运行效果

启动方式：
```bash
python app.py
```
访问地址：http://127.0.0.1:5000

页面效果：
- 顶部标题清晰
- 6个模块整齐排列
- 图表加载迅速、交互流畅
- 关联规则表格美观易读

### 8. 系统特点与创新点

1. **前后端分离**：接口与展示完全解耦，便于后续开发移动端或替换前端框架。
2. **智能“其他”类别处理**：品牌饼图避免长尾碎片化。
3. **关联规则深度优化**：
   - 列名美化
   - 双向规则去重
   - 多维度阈值过滤
   - 视觉化颜色提示
4. **用户体验友好**：空数据处理、加载提示、响应式布局。

### 9. 系统不足与改进方向

#### 9.1 现有不足
- 数据量较大时关联规则计算较慢（Apriori算法本身复杂度较高）。
- 未实现分页或实时刷新。
- 未加入用户认证与权限管理。
- 未使用地图展示地域分布。

#### 9.2 未来改进方向
1. 算法优化：改用FP-Growth算法提升挖掘效率。
2. 加入ECharts地图组件，实现省份/城市热力图。
3. 支持动态筛选（如选择年份范围后刷新图表）。
4. 部署到云服务器（如阿里云、Vercel+Flask），提供在线访问。
5. 增加更多维度（如价格区间、颜色、使用性质等）的挖掘。
6. 前端框架升级（如使用Vue/React提升交互性）。

### 10. 总结

本系统成功实现了基于Flask与ECharts的车辆数据可视化分析平台，不仅提供了直观的统计图表，还通过关联规则挖掘揭示了数据背后的深层模式。系统架构清晰、代码规范、界面美观，具有较强的实用性和可扩展性，达到了预期设计目标。

感谢指导老师及同学的帮助！

**项目完成人**： [你的姓名]  
**完成日期**： 2025年12月

---

这份文档已经完整涵盖了从背景到实现、从功能到优化的所有内容，适合作为毕业设计文档、课程报告或答辩配套材料直接使用。你可以直接复制到Word或Markdown中排版，插入运行截图后会更加完美。祝你答辩顺利！如果需要添加截图说明、代码注释版本或PPT提纲，再告诉我。
